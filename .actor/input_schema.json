{
  "title": "Caterer Job Scraper",
  "description": "Scrape hospitality and catering jobs from Caterer.com. Provide either keyword/location OR a Caterer.com search URL (startUrl, url, or startUrls). Actor paginates and optionally scrapes detail pages.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrl": {
      "title": "Start URL",
      "type": "string",
      "description": "Start scraping from a specific Caterer.com search URL. If provided, this will be used instead of Keyword/Location.",
      "editor": "textfield"
    },
    "keyword": {
      "title": "Keyword (if not using Start URL)",
      "type": "string",
      "description": "Job search keyword(s), e.g., 'Head Chef', 'Restaurant Manager', 'Sous Chef', 'Bartender'.",
      "editor": "textfield"
    },
    "location": {
      "title": "Location (if not using Start URL)",
      "type": "string",
      "description": "Location to search for jobs (e.g., 'London', 'Manchester', 'Birmingham'). Leave empty for all locations.",
      "editor": "textfield"
    },
    "postedWithin": {
      "title": "Jobs posted within",
      "type": "string",
      "description": "Only keep jobs posted within this time window.",
      "enum": [
        "any",
        "24h",
        "7d",
        "30d"
      ],
      "enumTitles": [
        "Any time",
        "Last 24 hours",
        "Last 7 days",
        "Last 30 days"
      ],
      "default": "any"
    },
    "collectDetails": {
      "title": "Collect job details",
      "type": "boolean",
      "description": "If enabled, scraper opens job detail pages for full description and additional information.",
      "editor": "checkbox",
      "default": true
    },
    "results_wanted": {
      "title": "Maximum number of jobs",
      "type": "integer",
      "description": "The maximum number of jobs to collect. Leave empty to collect all available jobs.",
      "minimum": 1,
      "default": 100,
      "editor": "number"
    },
    "max_pages": {
      "title": "Maximum number of pages",
      "type": "integer",
      "description": "A safety cap on the number of search result pages to visit.",
      "minimum": 1,
      "editor": "number",
      "default": 20
    },
    "debugSaveHtml": {
      "title": "Save first page HTML when empty",
      "type": "boolean",
      "description": "When enabled, stores the first list page HTML in KEY_VALUE_STORE (key DEBUG_LIST_PAGE) if no jobs are found or the response looks blocked.",
      "editor": "checkbox",
      "default": false
    },
    "proxyConfiguration": {
      "title": "Proxy configuration",
      "type": "object",
      "description": "Use Apify Proxy (datacenter recommended for Caterer.com).",
      "editor": "proxy"
    }
  },
  "required": [],
  "additionalProperties": true
}
