{
  "title": "Caterer Job Scraper",
  "description": "Scrape hospitality and catering jobs from Caterer.com. Uses hybrid Playwright + HTTP approach for maximum speed and stealth.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrl": {
      "title": "Start URL",
      "type": "string",
      "description": "Start scraping from a specific Caterer.com search URL. If provided, this will be used instead of Keyword/Location.",
      "editor": "textfield"
    },
    "keyword": {
      "title": "Keyword",
      "type": "string",
      "description": "Job search keyword(s), e.g., 'Head Chef', 'Restaurant Manager', 'Sous Chef', 'Bartender'.",
      "editor": "textfield",
      "prefill": "chef"
    },
    "location": {
      "title": "Location",
      "type": "string",
      "description": "Location to search for jobs (e.g., 'London', 'Manchester', 'Birmingham'). Leave empty for all locations.",
      "editor": "textfield",
      "prefill": "london"
    },
    "results_wanted": {
      "title": "Maximum number of jobs",
      "type": "integer",
      "description": "The maximum number of jobs to collect.",
      "minimum": 1,
      "maximum": 500,
      "default": 20,
      "editor": "number"
    },
    "max_pages": {
      "title": "Maximum number of pages",
      "type": "integer",
      "description": "A safety cap on the number of search result pages to visit.",
      "minimum": 1,
      "maximum": 20,
      "editor": "number",
      "default": 5
    },
    "max_detail_concurrency": {
      "title": "Detail page concurrency",
      "type": "integer",
      "description": "Number of parallel requests for fetching job details. Lower = stealthier, higher = faster.",
      "minimum": 1,
      "maximum": 5,
      "default": 3,
      "editor": "number"
    },
    "proxyConfiguration": {
      "title": "Proxy configuration",
      "type": "object",
      "description": "Use Apify Proxy (residential recommended for Caterer.com).",
      "editor": "proxy",
      "sectionCaption": "Proxy settings",
      "sectionDescription": "Residential proxies with GB country code are strongly recommended for reliable scraping."
    }
  },
  "required": [],
  "additionalProperties": false
}